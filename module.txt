1.why should we use module.
	使用模块能给我们带来许多好处，比如：
 保持全局命名空间的清洁；
 编写测试代码更容易，并能保持其清洁，以便更容易找到互相隔离的功能；
 易于在不同应用间复用代码；
 使应用能够以任意顺序加载代码的各个部分


参数
下面是angular.module()的参数列表。
3.1.1 name（字符串）
name是模块的名称，字符串变量。
3.1.2 requires（字符串数组）
requires包含了一个字符串变量组成的列表，每个元素都是一个模块名称，本模块依赖于这
些模块，依赖需要在本模块加载之前由注入器进行预加载
